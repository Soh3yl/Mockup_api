# 🧢 Mockup API

## 📖 Description

این پروژه یک پروژه‌ی بک‌اند نوشته‌شده با **Django REST Framework + Celery + Redis** است  
که به کاربر اجازه می‌دهد متن دلخواه خود را روی تصاویر تی‌شرت با رنگ‌های مختلف چاپ کند 🎨  

این پروژه شامل احراز هویت JWT، پردازش پس‌زمینه با Celery، و مستندسازی کامل با Swagger است.  
همچنین به‌صورت کامل **Dockerized** شده و تنها با یک دستور می‌توانید آن را اجرا کنید.

---
---

## 📦 ویژگی‌ها

- احراز هویت JWT (Login / Register / Logout)
- تولید تصویر موکاپ با Celery (پردازش غیربلوکی)
- پشتیبانی از رنگ و فونت سفارشی
- تاریخچه‌ی موکاپ‌ها با Pagination و Search
- Swagger UI برای مستندسازی
- Docker Compose برای اجرای سریع پروژه

---

## ⚙️ پیش‌نیازها

- Python 3.10+
- Redis
- Docker (اختیاری اما توصیه‌شده)
- pip و virtualenv (در حالت بدون Docker)

---

## 🚀 اجرای پروژه با Docker (ساده‌ترین روش)

### 1. اطمینان از نصب Docker و Docker Compose

### 2. در مسیر ریشه پروژه (`/`) اجرا کنید:

```bash
docker-compose up --build
```

بعد از اجرا، سرویس‌ها فعال می‌شوند:

- **Django:** http://127.0.0.1:8000
- **Swagger Docs:** http://127.0.0.1:8000/api/docs/swagger/

### 3. توقف پروژه:

```bash
docker-compose down
```

---

## 🧰 اجرای پروژه بدون Docker

### 1️⃣ ساخت محیط مجازی

```bash
python -m venv venv
source venv/bin/activate  # در ویندوز: venv\Scripts\activate
```

### 2️⃣ نصب نیازمندی‌ها

```bash
pip install -r requirements.txt
```

### 3️⃣ اجرای Redis

```bash
redis-server
```

### 4️⃣ اجرای Celery worker

در ترمینال جدا:

```bash
celery -A core worker -l info
```

### 5️⃣ اجرای Django server

```bash
python manage.py migrate
python manage.py runserver
```

---

## 📚 آدرس‌های مهم

| مسیر | توضیح |
|------|-------|
| `/api/auth/register/` | ثبت‌نام کاربر |
| `/api/auth/login/` | دریافت توکن JWT |
| `/api/auth/logout/` | خروج کاربر |
| `/api/v1/mockups/generate/` | ایجاد تسک ساخت موکاپ |
| `/api/v1/tasks/<task_id>/` | بررسی وضعیت تسک |
| `/api/v1/mockups/` | مشاهده تاریخچه با Pagination و Search |
| `/api/docs/swagger/` | Swagger UI (مستندات کامل) |

---

## 📷 نمونه درخواست ساخت موکاپ

### درخواست:

```http
POST /api/v1/mockups/generate/
Content-Type: application/json

{
  "text": "This is Demo",
  "font": "vazir",
  "text_color": "#FF0000",
  "shirt_color": ["white", "black"]
}
```

### پاسخ:

```json
{
  "task_ids": ["uuid-12345"],
  "status": "PENDING",
  "message": "ساخت تصویر آغاز شد"
}
```

---

## ⚡ تکنولوژی‌های استفاده‌شده

- Django REST Framework
- Celery + Redis
- drf-spectacular (Swagger)
- Docker Compose
- Pillow (برای ساخت تصویر)

---

## 📝 نکات تکمیلی

- برای دسترسی به Admin Panel: http://127.0.0.1:8000/admin/
- فایل‌های تولید شده در پوشه `media/mockups/` ذخیره می‌شوند
